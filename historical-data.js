// In-season snapshot of the CURRENT season's games/results/picks.
// Regenerate from the browser console with exportHistoricalData() and paste the output here.
//
// HISTORICAL_DATA_SEASON tags which season this snapshot belongs to. app.js clears the
// data automatically when CURRENT_SEASON moves past it (the season rolls over every
// July 1st), so a stale snapshot can never leak into a new season.
//
// The 2025 season's full data (including playoff weeks 19-22) lives in historical-2025.js.

const HISTORICAL_DATA_SEASON = 2026;

const HISTORICAL_GAMES = {};

const HISTORICAL_RESULTS = {};

const HISTORICAL_PICKS = {};
